class MinhaClasse {
    int x;
    bool y;

    int soma(int a, int b) {
        return a + b;
    }

    void metodo(int a, bool b) {
        if (a > 10) {
            return true;
        } else {
            return false;
        }
    }

    void main() {
        int a = 5;
        bool b = true;
        int c = soma(a, 10);
        metodo(a, b);
        return a + b;
    }
}
